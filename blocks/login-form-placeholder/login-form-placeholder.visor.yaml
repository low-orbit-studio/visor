name: Login Form Placeholder
description: A placeholder login form block using Visor form components (Button, Input, Label, Card).
category: authentication
components_used:
  - button
  - input
  - label
  - card
when_to_use:
  - Testing blocks infrastructure end-to-end
  - Scaffolding a login page before building the real block
when_not_to_use:
  - Production authentication flows (use a real login block)
