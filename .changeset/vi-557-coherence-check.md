---
"@loworbitstudio/visor": minor
---

Add CoherenceCheck component: `CheckGroup` (uppercase group header) + `CheckRow` (pass/warn/fail state icon circle, title, description with inline `code` token support, right-aligned ghost Fix action). Covers the Brand Workbench Prove stage coherence audit UI.
