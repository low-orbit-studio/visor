---
"@loworbitstudio/visor-core": minor
---

Add `--layout-content-max-width` (1200px) and `--layout-page-max-width` (1440px) semantic tokens to the `visor-semantic` cascade layer. Themes can override these via flat-key overrides in their `.visor.yaml`.
