---
"@loworbitstudio/visor": minor
---

Add `EditableBlock` component — canvas brand block with inline edit and AI affordance. Board tile with uppercase label header (+ done check), value body, hover-revealed edit icon, editing state (focus ring, inline input + save button, AI action slot). Covers Brand Workbench Canvas stage blocks.
