name: DropdownMenu
description: Menu of actions or options that opens from a trigger button.
extends: HTMLDivElement
category: overlay

when_to_use:
  - Actions menu on a card, row, or toolbar (edit, delete, duplicate)
  - User account menus with navigation and sign-out
  - Contextual options where space is limited
  - Grouped actions with sub-menus, checkboxes, or radio selections

when_not_to_use:
  - Right-click context menus (use ContextMenu)
  - Form field selection from a list (use Select or Combobox)
  - Horizontal app-level menu bar (use Menubar)
  - Floating rich content (use Popover)

why: >
  DropdownMenu wraps Radix UI DropdownMenu with Visor token styling, providing
  full keyboard navigation, focus management, and sub-menu support. It includes
  checkbox and radio item variants for stateful selections, destructive item
  styling, keyboard shortcut display, and animated enter/exit transitions —
  all themed via CSS custom properties.

density_axis: >
  Editorial admin treatment is activated by placing data-density="editorial"
  on any ancestor (typically the app root). Editorial values — Content: 1px gap,
  radius-md, surface-elev background with color-mix fallback, 6px padding, inset
  hairline ring. Item: 13px font, 8px 10px padding, spacing-3 gap, radius-sm,
  text-tertiary leading icon at 16px, surface-subtle hover, color-mix 14%
  destructive hover. Label: 11px uppercase 0.08em tracking text-tertiary
  6px 10px 2px padding. Shortcut: 11px text-muted 0.04em tracking. Separator:
  hairline color, 4px 2px margin. Default (no attribute) rendering is
  byte-identical to the pre-density canonical fallbacks.

sub_components:
  - name: DropdownMenu
    description: Root component managing open/close state.
  - name: DropdownMenuTrigger
    description: Button that opens the menu.
  - name: DropdownMenuContent
    description: The floating menu container. Accepts variant="breakout" for scroll-clipped contexts.
  - name: DropdownMenuItem
    description: A clickable menu action.
  - name: DropdownMenuCheckboxItem
    description: A toggleable checkbox item.
  - name: DropdownMenuRadioGroup
    description: Container for mutually exclusive radio items.
  - name: DropdownMenuRadioItem
    description: A selectable radio option.
  - name: DropdownMenuLabel
    description: Non-interactive group heading.
  - name: DropdownMenuSeparator
    description: Visual divider between groups.
  - name: DropdownMenuShortcut
    description: Keyboard shortcut hint aligned to the right.
  - name: DropdownMenuGroup
    description: Logical grouping of related items.
  - name: DropdownMenuSub
    description: Root for a nested sub-menu.
  - name: DropdownMenuSubTrigger
    description: Item that opens a sub-menu on hover.
  - name: DropdownMenuSubContent
    description: The nested sub-menu container. Accepts variant="breakout" for scroll-clipped contexts.
  - name: DropdownMenuPortal
    description: Portal wrapper for rendering outside the DOM tree.

props:
  - name: inset
    type: boolean
    default: false
    description: Adds left padding to align with items that have indicators
  - name: variant
    type: "'default' | 'destructive'"
    default: "default"
    description: Visual style for the menu item (DropdownMenuItem)
  - name: variant (DropdownMenuContent / DropdownMenuSubContent)
    type: "'default' | 'breakout'"
    default: "default"
    description: >
      Elevation variant for the menu container. Use "breakout" inside scroll-clipped
      containers like DataTable rows, sticky toolbars, or any overflow:hidden ancestor.
      The breakout variant raises z-index to 200 and deepens the shadow so the menu
      escapes the stacking context. The "default" value is byte-identical to
      pre-variant rendering — no consumer migration required.

dependencies:
  - "@radix-ui/react-dropdown-menu"
  - "@phosphor-icons/react"

preview_url: "https://visor.loworbit.studio/docs/components/overlay/dropdown-menu"

example: |
  <DropdownMenu>
    <DropdownMenuTrigger asChild>
      <Button variant="ghost">Actions</Button>
    </DropdownMenuTrigger>
    <DropdownMenuContent>
      <DropdownMenuItem>Edit</DropdownMenuItem>
      <DropdownMenuItem>Duplicate</DropdownMenuItem>
      <DropdownMenuSeparator />
      <DropdownMenuItem variant="destructive">Delete</DropdownMenuItem>
    </DropdownMenuContent>
  </DropdownMenu>
