---
title: Heading
description: A semantic heading component (h1–h6) with independent size and weight variants. Decouples visual presentation from semantic level.
---

import { ComponentPreview } from '@/components/preview';
import { PropsTable } from '@/components/props-table';
import { Heading } from '@/components/ui/heading';

## Installation

```bash
npx visor add heading
```

This copies two files into your project:

- `components/ui/heading/heading.tsx` — the component
- `components/ui/heading/heading.module.css` — the styles

## Usage

```tsx
import { Heading } from '@/components/ui/heading/heading';

export default function Example() {
  return <Heading level={1}>Page Title</Heading>;
}
```

## Levels

Each `level` renders the corresponding HTML heading element (`<h1>` through `<h6>`) with an auto-mapped visual size.

<ComponentPreview
  title="All Levels"
  code={`import { Heading } from '@/components/ui/heading/heading';

<Heading level={1}>Heading 1</Heading>
<Heading level={2}>Heading 2</Heading>
<Heading level={3}>Heading 3</Heading>
<Heading level={4}>Heading 4</Heading>
<Heading level={5}>Heading 5</Heading>
<Heading level={6}>Heading 6</Heading>`}
>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
    <Heading level={1}>Heading 1</Heading>
    <Heading level={2}>Heading 2</Heading>
    <Heading level={3}>Heading 3</Heading>
    <Heading level={4}>Heading 4</Heading>
    <Heading level={5}>Heading 5</Heading>
    <Heading level={6}>Heading 6</Heading>
  </div>
</ComponentPreview>

## Size Override

The `size` prop lets you decouple visual size from semantic level — an `<h3>` can look like an `<h1>`.

<ComponentPreview
  title="Size Override"
  code={`<Heading level={3} size="2xl">h3 that looks like h1</Heading>
<Heading level={1} size="sm">h1 that looks like h5</Heading>`}
>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
    <Heading level={3} size="2xl">h3 that looks like h1</Heading>
    <Heading level={1} size="sm">h1 that looks like h5</Heading>
  </div>
</ComponentPreview>

## Weights

<ComponentPreview
  title="Weight Variants"
  code={`<Heading weight="normal">Normal Weight</Heading>
<Heading weight="medium">Medium Weight</Heading>
<Heading weight="semibold">Semibold Weight</Heading>
<Heading weight="bold">Bold Weight</Heading>`}
>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
    <Heading weight="normal">Normal Weight</Heading>
    <Heading weight="medium">Medium Weight</Heading>
    <Heading weight="semibold">Semibold Weight</Heading>
    <Heading weight="bold">Bold Weight</Heading>
  </div>
</ComponentPreview>

## API Reference

### HeadingProps

<PropsTable component="heading" />

The component also accepts all standard HTML heading attributes.
