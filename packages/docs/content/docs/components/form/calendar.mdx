---
title: Calendar
description: A calendar component wrapping react-day-picker with Visor token styling and Phosphor icon navigation.
---

import { ComponentPreview } from '@/components/preview';
import { PropsTable } from '@/components/props-table';
import { Calendar } from '@/components/ui/calendar';

## Single Selection

<ComponentPreview
  title="Single Date"
  code={"import { Calendar } from '@/components/ui/calendar/calendar';\n\n<Calendar mode=\"single\" />"}
>
  <Calendar mode="single" />
</ComponentPreview>

## Range Selection

<ComponentPreview
  title="Date Range"
  code={"import { Calendar } from '@/components/ui/calendar/calendar';\n\n<Calendar mode=\"range\" />"}
>
  <Calendar mode="range" />
</ComponentPreview>

## Multiple Selection

<ComponentPreview
  title="Multiple Dates"
  code={"import { Calendar } from '@/components/ui/calendar/calendar';\n\n<Calendar mode=\"multiple\" />"}
>
  <Calendar mode="multiple" />
</ComponentPreview>

## With Disabled Dates

<ComponentPreview
  title="Disabled Dates"
  code={"import { Calendar } from '@/components/ui/calendar/calendar';\n\n<Calendar\n  mode=\"single\"\n  disabled={{ dayOfWeek: [0, 6] }}\n/>"}
>
  <Calendar
    mode="single"
    disabled={{ dayOfWeek: [0, 6] }}
  />
</ComponentPreview>

## Installation

```bash
npx visor add calendar
```

This copies two files into your project:

- `components/ui/calendar/calendar.tsx` — the component
- `components/ui/calendar/calendar.module.css` — the styles

## Usage

```tsx
import { Calendar } from '@/components/ui/calendar/calendar';
import { useState } from 'react';

export default function Example() {
  const [date, setDate] = useState<Date | undefined>(new Date());

  return (
    <Calendar
      mode="single"
      selected={date}
      onSelect={setDate}
    />
  );
}
```

## API Reference

<PropsTable component="calendar" />

The Calendar component accepts all [react-day-picker](https://react-day-picker.js.org/) props including `mode`, `selected`, `onSelect`, `disabled`, `fromDate`, `toDate`, and more.

## Accessibility

- Built on `react-day-picker` which renders a full ARIA grid calendar
- Navigation buttons include `aria-label` attributes ("Go to previous month", "Go to next month")
- Days communicate their state via `aria-selected`, `aria-disabled`, and `aria-current="date"` for today
- Full keyboard navigation: Arrow keys move focus between days, Enter/Space select, Page Up/Down navigate months
- Announce the current month/year via `role="heading"` on the caption
