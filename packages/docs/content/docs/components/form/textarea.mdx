---
title: Textarea
description: A multi-line text input with focus and validation states.
---

import { ComponentPreview } from '@/components/preview';
import { PropsTable } from '@/components/props-table';
import { Textarea } from '@/components/ui/textarea';
import { Field, FieldLabel, FieldError } from '@/components/ui/field';

## Default

<ComponentPreview
  title="Textarea"
  code={"import { Textarea } from '@/components/ui/textarea/textarea';\n\n<Textarea placeholder=\"Type your message here.\" />"}
>
  <div style={{ width: '100%', maxWidth: '20rem' }}>
    <Textarea placeholder="Type your message here." />
  </div>
</ComponentPreview>

## Custom Rows

<ComponentPreview
  title="Custom Rows"
  code={"<Textarea placeholder=\"Detailed description...\" rows={6} />"}
>
  <div style={{ width: '100%', maxWidth: '20rem' }}>
    <Textarea placeholder="Detailed description..." rows={6} />
  </div>
</ComponentPreview>

## Disabled

<ComponentPreview
  title="Disabled"
  code={"<Textarea disabled placeholder=\"Disabled textarea\" />"}
>
  <div style={{ width: '100%', maxWidth: '20rem' }}>
    <Textarea disabled placeholder="Disabled textarea" />
  </div>
</ComponentPreview>

## Sizes

<ComponentPreview
  title="Sizes"
  code={"<Textarea size=\"sm\" placeholder=\"Small\" />\n<Textarea size=\"md\" placeholder=\"Medium (default)\" />\n<Textarea size=\"lg\" placeholder=\"Large\" />"}
>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem', width: '100%', maxWidth: '20rem' }}>
    <Textarea size="sm" placeholder="Small" />
    <Textarea size="md" placeholder="Medium (default)" />
    <Textarea size="lg" placeholder="Large" />
  </div>
</ComponentPreview>

## Invalid

<ComponentPreview
  title="Invalid"
  code={"<Textarea aria-invalid=\"true\" defaultValue=\"This field has an error.\" />"}
>
  <div style={{ width: '100%', maxWidth: '20rem' }}>
    <Textarea aria-invalid="true" defaultValue="This field has an error." />
  </div>
</ComponentPreview>

## With Field

<ComponentPreview
  title="With Field"
  code={"import { Field, FieldLabel, FieldError } from '@/components/ui/field/field';\nimport { Textarea } from '@/components/ui/textarea/textarea';\n\n<Field>\n  <FieldLabel htmlFor=\"message\">Message</FieldLabel>\n  <Textarea id=\"message\" placeholder=\"Type your message here.\" aria-invalid=\"true\" />\n  <FieldError errors={[\"Message is required.\"]} />\n</Field>"}
>
  <div style={{ width: '100%', maxWidth: '20rem' }}>
    <Field>
      <FieldLabel htmlFor="message-field">Message</FieldLabel>
      <Textarea id="message-field" placeholder="Type your message here." aria-invalid="true" />
      <FieldError errors={["Message is required."]} />
    </Field>
  </div>
</ComponentPreview>

## Installation

```bash
npx visor add textarea
```

This copies two files into your project:

- `components/ui/textarea/textarea.tsx` — the component
- `components/ui/textarea/textarea.module.css` — the styles

## Usage

```tsx
import { Textarea } from '@/components/ui/textarea/textarea';

<Textarea placeholder="Type your message here." />
```

## API Reference

### TextareaProps

<PropsTable component="textarea" />

The component also accepts all standard `<textarea>` HTML attributes.

## Accessibility

- Renders a native `<textarea>` element — always pair with a `<Label>` using matching `htmlFor`/`id`
- Use `aria-invalid="true"` to signal a validation error; pair with a `<FieldError>` or `aria-describedby` pointing to the error message
- Use the `Field` component to automatically compose label, description, and error for a fully accessible form field
- Consider adding a character count display with `aria-live="polite"` when using `maxLength`
