---
title: Input
description: A text input component with focus and validation states.
---

import { ComponentPreview } from '@/components/preview';
import { PropsTable } from '@/components/props-table';
import { Input } from '@/components/ui/input';
import { Field, FieldLabel, FieldError } from '@/components/ui/field';
import { InputLeadingIconDemo } from '@/components/ui/input-leading-icon-demo';

## Default

<ComponentPreview
  title="Input"
  code={"import { Input } from '@/components/ui/input/input';\n\n<Input type=\"email\" placeholder=\"you@example.com\" />"}
>
  <div style={{ width: '100%', maxWidth: '20rem' }}>
    <Input type="email" placeholder="you@example.com" />
  </div>
</ComponentPreview>

## Types

<ComponentPreview
  title="Input Types"
  code={"<Input type=\"text\" placeholder=\"Text\" />\n<Input type=\"email\" placeholder=\"Email\" />\n<Input type=\"number\" placeholder=\"Number\" />\n<Input type=\"url\" placeholder=\"URL\" />"}
>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem', width: '100%', maxWidth: '20rem' }}>
    <Input type="text" placeholder="Text" />
    <Input type="email" placeholder="Email" />
    <Input type="number" placeholder="Number" />
    <Input type="url" placeholder="URL" />
  </div>
</ComponentPreview>

## Disabled

<ComponentPreview
  title="Disabled"
  code={"<Input disabled placeholder=\"Disabled input\" />"}
>
  <div style={{ width: '100%', maxWidth: '20rem' }}>
    <Input disabled placeholder="Disabled input" />
  </div>
</ComponentPreview>

## Sizes

<ComponentPreview
  title="Sizes"
  code={"<Input size=\"sm\" placeholder=\"Small\" />\n<Input size=\"md\" placeholder=\"Medium (default)\" />\n<Input size=\"lg\" placeholder=\"Large\" />"}
>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem', width: '100%', maxWidth: '20rem' }}>
    <Input size="sm" placeholder="Small" />
    <Input size="md" placeholder="Medium (default)" />
    <Input size="lg" placeholder="Large" />
  </div>
</ComponentPreview>

## Leading icon

Pass any node (we use [Phosphor Icons](https://phosphoricons.com/)) as
`leadingIcon` to render an icon inside the field. The input reserves
enough left padding to clear the glyph, which scales in `em` with the
field's font-size.

<ComponentPreview
  title="Leading icon"
  code={"import { MagnifyingGlass, EnvelopeSimple, User } from '@phosphor-icons/react';\nimport { Input } from '@/components/ui/input/input';\n\n<Input leadingIcon={<MagnifyingGlass weight=\"bold\" />} placeholder=\"Search products\" />\n<Input leadingIcon={<EnvelopeSimple weight=\"bold\" />} type=\"email\" placeholder=\"you@example.com\" />\n<Input leadingIcon={<User weight=\"bold\" />} placeholder=\"Username\" />"}
>
  <InputLeadingIconDemo />
</ComponentPreview>

## Invalid

<ComponentPreview
  title="Invalid"
  code={"<Input aria-invalid=\"true\" defaultValue=\"bad-email\" />"}
>
  <div style={{ width: '100%', maxWidth: '20rem' }}>
    <Input aria-invalid="true" defaultValue="bad-email" />
  </div>
</ComponentPreview>

## Password managers

By default, `Input` opts out of password-manager autofill (1Password, Bitwarden,
LastPass) so contact, marketing, and settings forms don't render autofill icon
overlays. Browsers ignore `autocomplete="off"` on individual inputs, so Visor
emits the four per-manager `data-*` attributes (`data-1p-ignore`,
`data-bwignore`, `data-lpignore`, `data-form-type="other"`) that each manager
respects.

On login, signup, or credential fields where you *want* the manager to offer
autofill, opt back in with `passwordManagers="allow"`:

<ComponentPreview
  title="Login field opts back in"
  code={"<Input type=\"email\" placeholder=\"you@example.com\" passwordManagers=\"allow\" />\n<Input type=\"password\" passwordManagers=\"allow\" />"}
>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem', width: '100%', maxWidth: '20rem' }}>
    <Input type="email" placeholder="you@example.com" passwordManagers="allow" />
    <Input type="password" passwordManagers="allow" />
  </div>
</ComponentPreview>

## With Field

<ComponentPreview
  title="With Field"
  code={"import { Field, FieldLabel, FieldError } from '@/components/ui/field/field';\nimport { Input } from '@/components/ui/input/input';\n\n<Field>\n  <FieldLabel htmlFor=\"email\">Email address</FieldLabel>\n  <Input id=\"email\" type=\"email\" placeholder=\"you@example.com\" aria-invalid=\"true\" />\n  <FieldError errors={[\"Please enter a valid email address.\"]} />\n</Field>"}
>
  <div style={{ width: '100%', maxWidth: '20rem' }}>
    <Field>
      <FieldLabel htmlFor="email-field">Email address</FieldLabel>
      <Input id="email-field" type="email" placeholder="you@example.com" aria-invalid="true" />
      <FieldError errors={["Please enter a valid email address."]} />
    </Field>
  </div>
</ComponentPreview>

## Installation

```bash
npx visor add input
```

This copies two files into your project:

- `components/ui/input/input.tsx` — the component
- `components/ui/input/input.module.css` — the styles

## Usage

```tsx
import { Input } from '@/components/ui/input/input';

<Input type="email" placeholder="you@example.com" />
```

## API Reference

### InputProps

<PropsTable component="input" />

The component also accepts all standard `<input>` HTML attributes.

## Accessibility

- Renders a native `<input>` — always pair with a `<Label>` using matching `htmlFor`/`id`
- Use `aria-invalid="true"` to signal a validation error; pair with a `<FieldError>` or `aria-describedby` pointing to the error message
- `disabled` sets `aria-disabled` and prevents interaction; `readonly` allows focus but prevents editing
- Use the `Field` component to automatically compose label, description, and error for a fully accessible form field
