---
title: Number Input
description: A number input with increment/decrement buttons and min/max/step support.
---

import { ComponentPreview } from '@/components/preview';
import { PropsTable } from '@/components/props-table';
import { NumberInput } from '@/components/ui/number-input';

## Installation

```bash
npx visor add number-input
```

This copies two files into your project:

- `components/ui/number-input/number-input.tsx` — the component
- `components/ui/number-input/number-input.module.css` — the styles

## Usage

```tsx
import { NumberInput } from '@/components/ui/number-input/number-input';

<NumberInput defaultValue={1} min={0} max={10} />
```

## Default

<ComponentPreview
  title="Number Input"
  code={"import { NumberInput } from '@/components/ui/number-input/number-input';\n\n<NumberInput defaultValue={1} min={0} max={99} aria-label=\"Quantity\" />"}
>
  <NumberInput defaultValue={1} min={0} max={99} aria-label="Quantity" />
</ComponentPreview>

## With Step

<ComponentPreview
  title="Step of 5"
  code={"<NumberInput defaultValue={0} step={5} min={0} max={100} aria-label=\"Amount\" />"}
>
  <NumberInput defaultValue={0} step={5} min={0} max={100} aria-label="Amount" />
</ComponentPreview>

## With Min/Max Bounds

<ComponentPreview
  title="Bounded Range"
  code={"<NumberInput defaultValue={50} min={1} max={100} aria-label=\"Percentage\" />"}
>
  <NumberInput defaultValue={50} min={1} max={100} aria-label="Percentage" />
</ComponentPreview>

## Disabled

<ComponentPreview
  title="Disabled"
  code={"<NumberInput defaultValue={5} disabled aria-label=\"Disabled quantity\" />"}
>
  <NumberInput defaultValue={5} disabled aria-label="Disabled quantity" />
</ComponentPreview>

## API Reference

### NumberInputProps

<PropsTable component="number-input" />

## Accessibility

- Always provide an `aria-label` or pair with a `<Label>` using `htmlFor`/`id` — the component has no visible label by default
- Increment/decrement buttons have `aria-label="Increment"` and `aria-label="Decrement"`
- The input renders as `type="text"` with `inputmode="numeric"` for appropriate mobile keyboard
- Keyboard: Up/Down arrow keys increment/decrement the value; Tab navigates between the input and buttons
- `min`/`max` bounds are enforced both visually (buttons disable at bounds) and on input blur
