---
title: Fieldset
description: A semantic fieldset with styled legend for grouping related form fields.
---

import { ComponentPreview } from '@/components/preview';
import { PropsTable } from '@/components/props-table';
import { Fieldset, FieldsetLegend } from '@/components/ui/fieldset';
import { Field, FieldLabel } from '@/components/ui/field';
import { Input } from '@/components/ui/input';

## Installation

```bash
npx visor add fieldset
```

This copies two files into your project:

- `components/ui/fieldset/fieldset.tsx` — the component
- `components/ui/fieldset/fieldset.module.css` — the styles

## Usage

```tsx
import { Fieldset, FieldsetLegend } from '@/components/ui/fieldset/fieldset';

<Fieldset>
  <FieldsetLegend>Contact Information</FieldsetLegend>
  {/* form fields */}
</Fieldset>
```

## Default

<ComponentPreview
  title="Fieldset"
  code={"import { Fieldset, FieldsetLegend } from '@/components/ui/fieldset/fieldset';\nimport { Field, FieldLabel } from '@/components/ui/field/field';\nimport { Input } from '@/components/ui/input/input';\n\n<Fieldset>\n  <FieldsetLegend>Contact Information</FieldsetLegend>\n  <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem', marginTop: '0.5rem' }}>\n    <Field>\n      <FieldLabel htmlFor=\"fname\">First name</FieldLabel>\n      <Input id=\"fname\" placeholder=\"Jane\" />\n    </Field>\n    <Field>\n      <FieldLabel htmlFor=\"lname\">Last name</FieldLabel>\n      <Input id=\"lname\" placeholder=\"Doe\" />\n    </Field>\n  </div>\n</Fieldset>"}
>
  <div style={{ width: '100%', maxWidth: '24rem' }}>
    <Fieldset>
      <FieldsetLegend>Contact Information</FieldsetLegend>
      <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem', marginTop: '0.5rem' }}>
        <Field>
          <FieldLabel htmlFor="fname-demo">First name</FieldLabel>
          <Input id="fname-demo" placeholder="Jane" />
        </Field>
        <Field>
          <FieldLabel htmlFor="lname-demo">Last name</FieldLabel>
          <Input id="lname-demo" placeholder="Doe" />
        </Field>
      </div>
    </Fieldset>
  </div>
</ComponentPreview>

## Disabled

<ComponentPreview
  title="Disabled Fieldset"
  code={"<Fieldset disabled>\n  <FieldsetLegend>Shipping Address</FieldsetLegend>\n  <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem', marginTop: '0.5rem' }}>\n    <Field>\n      <FieldLabel htmlFor=\"street\">Street</FieldLabel>\n      <Input id=\"street\" placeholder=\"123 Main St\" />\n    </Field>\n    <Field>\n      <FieldLabel htmlFor=\"city\">City</FieldLabel>\n      <Input id=\"city\" placeholder=\"Brooklyn\" />\n    </Field>\n  </div>\n</Fieldset>"}
>
  <div style={{ width: '100%', maxWidth: '24rem' }}>
    <Fieldset disabled>
      <FieldsetLegend>Shipping Address</FieldsetLegend>
      <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem', marginTop: '0.5rem' }}>
        <Field>
          <FieldLabel htmlFor="street-demo">Street</FieldLabel>
          <Input id="street-demo" placeholder="123 Main St" />
        </Field>
        <Field>
          <FieldLabel htmlFor="city-demo">City</FieldLabel>
          <Input id="city-demo" placeholder="Brooklyn" />
        </Field>
      </div>
    </Fieldset>
  </div>
</ComponentPreview>

## API Reference

### FieldsetProps

<PropsTable component="fieldset" />

## Accessibility

- Renders a native `<fieldset>` element — semantically groups related controls
- `FieldsetLegend` renders a `<legend>`, which screen readers associate with all controls inside the fieldset
- Setting `disabled` on the `<fieldset>` propagates to all descendant form controls automatically via the browser
- Prefer `Fieldset` + `FieldsetLegend` over generic `<div>` wrappers when controls share a logical purpose (e.g., address fields, contact info)
