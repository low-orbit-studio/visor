---
title: Password Input
description: A password input with show/hide toggle and optional strength meter.
---

import { ComponentPreview } from '@/components/preview';
import { PropsTable } from '@/components/props-table';
import { PasswordInput } from '@/components/ui/password-input';
import { Field, FieldLabel, FieldError } from '@/components/ui/field';

## Default

<ComponentPreview
  title="Password Input"
  code={"import { PasswordInput } from '@/components/ui/password-input/password-input';\n\n<PasswordInput placeholder=\"Enter password\" />"}
>
  <div style={{ width: '100%', maxWidth: '20rem' }}>
    <PasswordInput placeholder="Enter password" />
  </div>
</ComponentPreview>

## With Strength Meter

<ComponentPreview
  title="With Strength Meter"
  code={"<PasswordInput placeholder=\"Choose a strong password\" showStrength />"}
>
  <div style={{ width: '100%', maxWidth: '20rem' }}>
    <PasswordInput placeholder="Choose a strong password" showStrength />
  </div>
</ComponentPreview>

## Sizes

<ComponentPreview
  title="Sizes"
  code={"<PasswordInput size=\"sm\" placeholder=\"Small\" />\n<PasswordInput size=\"md\" placeholder=\"Medium (default)\" />\n<PasswordInput size=\"lg\" placeholder=\"Large\" />"}
>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem', width: '100%', maxWidth: '20rem' }}>
    <PasswordInput size="sm" placeholder="Small" />
    <PasswordInput size="md" placeholder="Medium (default)" />
    <PasswordInput size="lg" placeholder="Large" />
  </div>
</ComponentPreview>

## Disabled

<ComponentPreview
  title="Disabled"
  code={"<PasswordInput placeholder=\"Password\" disabled />"}
>
  <div style={{ width: '100%', maxWidth: '20rem' }}>
    <PasswordInput placeholder="Password" disabled />
  </div>
</ComponentPreview>

## Invalid

<ComponentPreview
  title="Invalid"
  code={"<PasswordInput aria-invalid=\"true\" defaultValue=\"123\" />"}
>
  <div style={{ width: '100%', maxWidth: '20rem' }}>
    <PasswordInput aria-invalid="true" defaultValue="123" />
  </div>
</ComponentPreview>

## With Field

<ComponentPreview
  title="With Field"
  code={"import { Field, FieldLabel, FieldError } from '@/components/ui/field/field';\nimport { PasswordInput } from '@/components/ui/password-input/password-input';\n\n<Field>\n  <FieldLabel htmlFor=\"password\">Password</FieldLabel>\n  <PasswordInput id=\"password\" placeholder=\"Enter password\" showStrength aria-invalid=\"true\" />\n  <FieldError errors={[\"Password must be at least 8 characters.\"]} />\n</Field>"}
>
  <div style={{ width: '100%', maxWidth: '20rem' }}>
    <Field>
      <FieldLabel htmlFor="password-field">Password</FieldLabel>
      <PasswordInput id="password-field" placeholder="Enter password" showStrength aria-invalid="true" />
      <FieldError errors={["Password must be at least 8 characters."]} />
    </Field>
  </div>
</ComponentPreview>

## Installation

```bash
npx visor add password-input
```

This copies two files into your project:

- `components/ui/password-input/password-input.tsx` — the component
- `components/ui/password-input/password-input.module.css` — the styles

This also installs the `input` component as a dependency.

## Usage

```tsx
import { PasswordInput } from '@/components/ui/password-input/password-input';

<PasswordInput placeholder="Enter password" />
```

## API Reference

### PasswordInputProps

<PropsTable component="password-input" />

The component also accepts all standard `<input>` HTML attributes.

## Accessibility

- The show/hide toggle button has `aria-label="Show password"` / `"Hide password"` that updates as state changes
- The toggle button uses `aria-pressed` to communicate the current visibility state to screen readers
- Pair with a `<Label>` using `htmlFor`/`id` for proper labeling
- When `showStrength` is enabled, the strength meter is announced via `aria-live="polite"` as the user types
- The input renders as `type="password"` by default; toggling to `type="text"` reveals the value
