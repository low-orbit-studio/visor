/**
 * NextJS starter template for visor init --template nextjs.
 *
 * Provides a minimal .visor.yaml that proves the end-to-end
 * theme pipeline works: .visor.yaml → adapter → globals.css.
 */

export const NEXTJS_STARTER_YAML = `\
name: my-app
version: 1
colors:
  primary: "#2563EB"
`;
